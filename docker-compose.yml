version: '3.8'

services:
  flow2api:
    image: ghcr.io/thesmallhancat/flow2api:latest
    container_name: flow2api
    ports:
      - "38000:8000"
    volumes:
      - ./data:/app/data
      - ./tmp:/app/tmp
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
